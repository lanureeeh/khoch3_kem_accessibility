# Docs (GitHub Pages root)

- `index.html`: Leaflet accessibility map entry point
- `data/*.geojson`: generated map layers for the web app

Live deployment:
- <https://lanureeeh.github.io/KEM_accessibility/>

Local preview:
- Run `python -m http.server 8000` from the repository root
- Open <http://localhost:8000/docs/>
